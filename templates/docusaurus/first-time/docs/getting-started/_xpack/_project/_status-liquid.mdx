
{/* ------------------------------------------------------------------------ */}

The `{{packageScopedName}}` project is fully functional and stable.

{%- if packageWebsiteConfig.hasCli != "true" %}

This module is primarily intended to be included in projects
like the `xpm` CLI application.

{%- endif %}
