{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

{/* ------------------------------------------------------------------------ */}

<details>
<summary>Branches</summary>

The project uses multiple branches:

- `master`, {% if branchMain != "master" %}not actively used{% else %}with the latest stable version (default){% endif %}
{%- if branchMain == "xpack" %}
- `xpack`, with the latest stable version (default)
{%- endif %}
- `{{branchDevelopment}}`, with the current development version
- `{{branchWebsite}}`, with the current content of the website; pushes to this branch automatically trigger publishes the main website
{%- if branchWebpreview != "" %}
- `{{branchWebpreview}}`, with the current content of the preview website; pushes to this branch automatically trigger publishes the preview website
{%- endif %}

All development is done in the `{{branchDevelopment}}` branch, and contributions via
Pull Requests should be directed to this branch.

When new releases are published, the `{{branchDevelopment}}` branch is merged
into `{{branchMain}}`.

</details>
