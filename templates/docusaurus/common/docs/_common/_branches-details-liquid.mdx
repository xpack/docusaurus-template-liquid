{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

{/* ------------------------------------------------------------------------ */}

<details>
<summary>Branches</summary>

This project utilises multiple branches:

- `master`, {% if branchMain != "master" %}not actively used{% else %}containing the latest stable version (default){% endif %}
{%- if branchMain == "xpack" %}
- `xpack`, containing the latest stable version (default)
{%- endif %}
- `{{branchDevelopment}}`, containing the current development version
{%- if branchWebsite != branchMain %}
- `{{branchWebsite}}`, containing the current website content; pushes to this branch automatically trigger publication of the main website
{%- endif %}
{%- if branchWebpreview != branchDevelopment %}
- `{{branchWebpreview}}`, containing the current preview website content; pushes to this branch automatically trigger publication of the preview website
{%- endif %}

All development is conducted in the `{{branchDevelopment}}` branch, and contributions via
Pull Requests should be directed to this branch.

When new releases are published, the `{{branchDevelopment}}` branch is merged
into `{{branchMain}}`.

</details>
