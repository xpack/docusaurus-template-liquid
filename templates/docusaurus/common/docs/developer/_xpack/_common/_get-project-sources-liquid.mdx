{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

{/* ------------------------------------------------------------------------ */}

## Get project sources

The project is hosted on GitHub:

- https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}.git

The project uses multiple branches:

{% if hasBranchMaster == "true" %}- `master`, {% if packageConfig.hasEmptyMaster == "true" %}not actively used{% else %}with the latest stable version (default){% endif %}{% endif %}{% if hasBranchDevelopment == "true" %}
- `development`, with the current development version{% endif %}{% if hasBranchWebsite == "true" %}
- `website`, with the current content of the website; pushes to this branch automatically trigger publishes the main website{% endif %}{% if hasBranchWebpreview == "true" %}
- `webpreview`, with the current content of the preview website; pushes to this branch automatically trigger publishes the preview website{% endif %}

To clone the stable branch (`master`), run the following commands in a
terminal (on Windows use the Git Bash console):

```sh
mkdir ~/Work/npm-packages && cd ~/Work/npm-packages
git clone https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}.git {{githubProjectName}}.git
```

For **development** purposes, clone the `development` branch:

```sh
mkdir ~/Work/npm-packages && cd ~/Work/npm-packages
git clone \
  --branch development \
  https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}.git {{githubProjectName}}.git
```

Or, if the repo was already cloned:

```sh
git -C ~/Work/npm-packages/{{githubProjectName}}.git pull
```
