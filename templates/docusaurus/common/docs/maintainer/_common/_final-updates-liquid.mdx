{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

import CodeBlock from '@theme/CodeBlock';

{/* ------------------------------------------------------------------------ */}

## Final updates

### Update the `master` branch

In this Git repo:

- select the `{{branchMain}}` branch
- **merge** the `{{branchDevelopment}}` branch into the `{{branchMain}}` branch
- **push** the `{{branchMain}}` branch to GitHub

### Close milestone

In https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}/milestones:

- close the current milestone.

### Check npm package tags

```sh
npm dist-tag ls {{packageScopedName}}
```

### Tag the npm package as `latest`

When the release is considered stable, promote it as `latest`:

<CodeBlock language="sh"> {
`npm dist-tag add {{packageScopedName}}@{{releaseVersion}} latest
npm dist-tag ls {{packageScopedName}}
`} </CodeBlock>


