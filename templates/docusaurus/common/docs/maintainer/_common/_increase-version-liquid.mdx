{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

{/* ------------------------------------------------------------------------ */}

### Increase the version and update the top `package.json`

As required by npm projects, this one also utilises [SemVer](https://semver.org).

- Determine the next version (such as <code>{{releaseVersion}}</code>)
- Update the version in the top `package.json` file
{%- if releaseSubversion != "" %}
- Use the new version, suffixed by `.pre`,
such as <code>{{releaseVersion}}.pre</code>.
{%- else %}
- Use the new version, suffixed by `-pre`,
such as <code>{{releaseVersion}}-pre</code>.
{%- endif %}
