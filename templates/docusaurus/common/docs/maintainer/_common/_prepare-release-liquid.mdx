{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

{/* ------------------------------------------------------------------------ */}

### Prepare the release

{% if isTypeScript -%}
- Terminate all running tasks (**Terminal** → **Terminate Task...**)
{% endif -%}
- Switch to the `{{branchDevelopment}}` branch
{% if isTypeScript or isJavaScript -%}
- `npm run fix`
{% endif -%}
- If there are changes, **commit** all changes to the `{{branchDevelopment}}` branch
{% if isTypeScript or isJavaScript -%}
- `npm run test`
{% endif -%}
- Check the latest commits `npm run git-log`
- Update `CHANGELOG.md`, add a line such as _**\* v{{releaseVersion}} published on npmjs.com**_
- **Commit** with the message _**CHANGELOG: publish npm v{{releaseVersion}}**_
- Check the list of files included in the npm package:
  ```sh
  npm run npm-pack
  ```
- Possibly update `.npmignore`
