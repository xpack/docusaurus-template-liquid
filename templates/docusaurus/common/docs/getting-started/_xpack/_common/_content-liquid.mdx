{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

import ProjectOverview from '../_project/_overview.mdx';

{/* ------------------------------------------------------------------------ */}

<ProjectOverview />

{%- if packageWebsiteConfig.skipInstallCommand != "true" %}

## Install

{% if packageConfig.permalinkName %}[{{packageScopedName}}](https://www.npmjs.com/package/{{packageScopedName}}) is a npm package available{% else %}The {{longXpackName}} is a npm package available as [{{packageScopedName}}](https://www.npmjs.com/package/{{packageScopedName}}){% endif %}
from the npmjs.com public registry and can be installed via **npm**.

:::tip

The easiest way to get the install command is from the website's homepage.
Simply hover the mouse over the command, and a **copy button** will appear on
the right side. Click it to copy the command.

:::

For comprehensive installation instructions, please visit the
[Install Guide](/docs/install/) page.

{%- endif %}
