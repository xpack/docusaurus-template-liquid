{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

import DetailsReproducibleBuilds from '../_common/_details-reproducible-builds.mdx';
import WhatAreXpmPackages from '../_common/_what-are-xpm-packages.mdx';
import Features from '../_common/_features.mdx';
import Benefits from '../_common/_benefits.mdx';
import InstallPreamble from '../_common/_install-preamble.mdx';

import Overview from '../_project/_overview.mdx';
import OtherBenefits from '../_project/_other-benefits.mdx';
import UpgradeNotice from '../_project/_upgrade-notice.mdx';
import Compatibility from '../_project/_compatibility.mdx';

{/* ------------------------------------------------------------------------ */}


## Overview

{%- assign platforms_array = platforms | split: "," %}
{%- assign names_array = "" | split: "" %}

{%- for platform in platforms_array %}
{%- if platform == "win32-x64" %}{% assign names_array = names_array | concat: "Windows" %}{% endif %}
{%- if platform == "darwin-x64" or platform == "darwin-arm64" %}{% assign names_array = names_array | concat: "macOS" %}{% endif %}
{%- if platform == "linux-x64" or platform == "linux-arm64" or platform == "linux-arm" %}{% assign names_array = names_array | concat: "GNU/Linux" %}{% endif %}
{%- endfor %}

{%- assign names_array = names_array | uniq %}

**{{longXpackName}}** is a
standalone{% if names_array.size > 1 %}, cross-platform ({{ names_array | join: ", " }}){% else %} {{ names_array | first }}{% endif %} binary
distribution of {{packageConfig.descriptiveName}},
aimed at reproducible builds.

<DetailsReproducibleBuilds />

<Overview/>

The open source **{{longXpackName}}** project is hosted on GitHub as
[`{{githubProjectOrganization}}/{{githubProjectName}}`](https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}/);
it provides the platform specific binaries as
[release assets](https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}/releases).

In addition to the binary archives and the package metadata,
this project also includes the
[build scripts](https://github.com/{{githubProjectOrganization}}/{{githubProjectName}}/tree/xpack/build-assets/scripts).

<UpgradeNotice/>

<WhatAreXpmPackages />

<Features />

<Benefits />

<OtherBenefits/>

<Compatibility/>

{%- if packageWebsiteConfig.skipInstallCommand != "true" %}

## Install

<InstallPreamble />

The details for installing the **{{longXpackName}}** on different platforms are
provided in the
[Install Guide](/docs/install/) on each project's website.

{%- endif %}
