---

# DO NOT EDIT!
# Automatically generated from docusaurus-template-liquid/templates/docusaurus.

title: User's Guide{% if websiteConfig.usePluralGuides %}s{% endif %}
{%- if githubProjectOrganization == 'xpack' %}
description: {% if websiteConfig.userGuideDescription != '' %}{{websiteConfig.userGuideDescription}}{% else %}User information, including how to integrate it in a project, how to instantiate objects, how to call various functions.{% endif %}
{%- elsif githubProjectOrganization == "xpack-dev-tools" %}
description: {% if websiteConfig.userGuideDescription != '' %}{{websiteConfig.userGuideDescription}}{% else %}User information, including the versioning scheme, usage of shared libraries, and RPATH configuration on GNU/Linux.{% endif %}
{%- else %}
description: TODO
{%- endif %}
keywords:
  - xpack
  - {% if topConfig.permalinkName != '' %}{{topConfig.permalinkName}}{% else %}{{packageName}}{% endif %}
  - user
  - guide{% if isXpackBinary %}
  - versioning
  - shared
  - rpath{% endif %}

date: 2024-10-24 23:58:00 +0300

---

{%- if websiteConfig.hasCustomUser %}

import ProjectContent from './_project/_content.mdx';

{/* ------------------------------------------------------------------------ */}

{%- if websiteConfig.customUserTitle != '' %}

# {{websiteConfig.customUserTitle}}

{%- endif %}

<ProjectContent />

{%- else %}

{%- if isXpackBinary %}

import Content from './_common/_content.mdx';

{%- else %}

import Content from './_project/_content.mdx';

{%- endif %}

{/* ------------------------------------------------------------------------ */}

# How to utilise {{topConfig.preferredName}}

{%- if isXpackBinary or topConfig.hasCli %}

This page is intended for those who plan to integrate
**{{topConfig.preferredName}}** into their workflows.

{%- else %}

This page is intended for those who plan to utilise this
{% if isNpmBinary %}application{% else %}module{% endif %} within their
own projects.

{%- endif %}

<Content />

{%- endif %}

{%- if websiteConfig.hasTypedocApi %}

### Reference

For further details on the available class definitions, including all methods,
accessors, properties, etc.,
please refer to the TypeDoc
[API](/docs/api/) pages.

{%- endif %}
